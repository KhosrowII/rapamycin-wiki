// prisma/schema.prisma
datasource db {
  provider = "sqlite"
  url      = "file:./db.sqlite?mode=ro"   // ← add ?mode=ro
}




generator client {
  provider = "prisma-client-js"
}
model Trial {
  nct     String @id
  keyword String
  title   String
  start   String?
  status  String
}
