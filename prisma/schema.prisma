// prisma/schema.prisma
datasource db {
  provider = "sqlite"
  url      = "file:./db.sqlite"    // ← delete env() wrapper
}


generator client {
  provider = "prisma-client-js"
}
model Trial {
  nct     String @id
  keyword String
  title   String
  start   String?
  status  String
}
